import { inputMappings, outputMappings } from '../typings'

export const sendRawTransaction = (tx: inputMappings): outputMappings => {
    
}

export const ethCall = (call: inputMappings): outputMappings => {
    
}